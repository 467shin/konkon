import React from "react";

const LoginPage = () => {
  return (
    <div>
      <h1>LoginRequired!</h1>
      <input type="text" />
      <input type="password" />
      <div>
        <button>로그인</button>
        <button>회원가입</button>
      </div>
    </div>
  );
};

export default LoginPage;
